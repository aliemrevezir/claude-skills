# python-test-runner

Run and debug Python test pipelines using pytest or unittest. Handles dependency installation, test path selection, coverage reporting, and failure analysis. Also validates AI tool integrations by checking environment variables, API keys, and performing health checks for OpenAI, Anthropic, Google/Gemini, and custom proxy endpoints. Use when the user asks to run tests, check CI, measure coverage, troubleshoot failing test suites, or verify AI API configuration in Python projects.


## Usage

This skill is automatically discovered by Claude when relevant to your conversation.

To test it, ask Claude to perform tasks related to: Run and debug Python test pipelines using pytest or unittest. Handles dependency installation, test path selection, coverage reporting, and failure analysis. Also validates AI tool integrations by checking environment variables, API keys, and performing health checks for OpenAI, Anthropic, Google/Gemini, and custom proxy endpoints. Use when the user asks to run tests, check CI, measure coverage, troubleshoot failing test suites, or verify AI API configuration in Python projects.


## Files

- `SKILL.md` - Main skill file with instructions

## Configuration

- **Name**: python-test-runner
- **Description**: Run and debug Python test pipelines using pytest or unittest. Handles dependency installation, test path selection, coverage reporting, and failure analysis. Also validates AI tool integrations by checking environment variables, API keys, and performing health checks for OpenAI, Anthropic, Google/Gemini, and custom proxy endpoints. Use when the user asks to run tests, check CI, measure coverage, troubleshoot failing test suites, or verify AI API configuration in Python projects.


- **Allowed Tools**: ['Bash', 'Read', 'Write', 'Edit']