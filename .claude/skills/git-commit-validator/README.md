# git-commit-validator

Validates and formats git commit messages using Conventional Commits standards. Use when the user wants to commit changes, write a commit message, or perform git operations. Triggers on keywords like "git commit", "commit message", "conventional commits", and "stage changes".

## Usage

This skill is automatically discovered by Claude when relevant to your conversation.

To test it, ask Claude to perform tasks related to: Validates and formats git commit messages using Conventional Commits standards. Use when the user wants to commit changes, write a commit message, or perform git operations. Triggers on keywords like "git commit", "commit message", "conventional commits", and "stage changes".

## Files

- `SKILL.md` - Main skill file with instructions

## Configuration

- **Name**: git-commit-validator
- **Description**: Validates and formats git commit messages using Conventional Commits standards. Use when the user wants to commit changes, write a commit message, or perform git operations. Triggers on keywords like "git commit", "commit message", "conventional commits", and "stage changes".

- **Allowed Tools**: ['Bash', 'Read', 'Write', 'Edit']